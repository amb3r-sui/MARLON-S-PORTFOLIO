import type { Metadata } from "next";
import Link from "next/link";
import { ArrowRight, CheckCircle2 } from "lucide-react";
import { CTA } from "@/components/ui/CTA";
import { services } from "@/data/services";
export const metadata: Metadata = { title: "Services", description: "Workflow automation, AI agents, CRM automation, API integration, data synchronization, testing, and documentation services." };
export default function ServicesPage() { return <><section className="page-hero"><div className="container"><span className="eyebrow">Services</span><h1>Practical automation help for teams ready to simplify operations.</h1><p>I work as an independent AI Automation Specialist, helping map, build, test, and document reliable workflows.</p></div></section><section className="section no-top"><div className="container service-grid">{services.map((service, index) => <article className="service-card" key={service.title}><div className="service-num">{String(index + 1).padStart(2, "0")}</div><h2>{service.title}</h2><p className="service-summary">{service.summary}</p><div className="service-detail"><span>Business problem</span><p>{service.problem}</p></div><div className="service-detail"><span>What I can build</span><ul>{service.deliverables.map((item) => <li key={item}><CheckCircle2 />{item}</li>)}</ul></div><div className="tag-row">{service.tools.map((tool) => <span className="tag" key={tool}>{tool}</span>)}</div><div className="benefit"><strong>Expected benefit</strong><p>{service.benefit}</p></div><Link href={`/contact?service=${encodeURIComponent(service.title)}`} className="text-link">Discuss this service <ArrowRight size={17} /></Link></article>)}</div></section><CTA /></>; }
